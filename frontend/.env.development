# 前端访问后端 API 的基础路径
VITE_BASE_API=/api

# Docker 中后端服务的真实地址（Nginx proxy）
VITE_PROXY_TARGET=http://backend:8080

# 是否启用 Mock 数据
# true: 启用 Mock，当后端服务不可用时自动使用 Mock 数据
# false: 禁用 Mock，直接连接后端服务器
VITE_USE_MOCK=true

